default_strict_extensions: true

allowed_extensions:
  - .tif
  - .tiff
  - .geotiff
  - .vrt
  - .img
  - .jp2

# When empty, all paths are readable.
# Restrict this list in production environments.
allowed_roots: []
